# WorldQuant Scope 1.2.2

> [!IMPORTANT]
> 重要隐私预告：从 V1.3.0 起，插件安装或升级后将使用当前 WorldQuant 登录态读取并上传 WQ ID、国家/地区、插件版本、随机安装 ID 及升级来源，用于版本覆盖与用户分布统计。不会上传 Cookie 内容、密码、Session Token、API Key、Alpha、论坛内容或页面 URL。插件不会自动更新；如果不同意上述数据登记，请不要下载安装 V1.3.0 或后续版本。

V1.2.2 只发布上述提前公告并更新指南、日志与发布流程。本版本不包含统计端点、数据上传逻辑或后台登记请求。

版本号遵循 `x.y.z`：`x` 为重大架构变更，`y` 为功能新增，`z` 为修复或文档更新。
